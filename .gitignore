# BGM の元ファイル(DOVA-SYNDROME からダウンロードした mp3)。
# 公開リポジトリに素材そのものを置くと再配布に当たりうるので入れない。
# 公開用に変換した assets/bgm/*.m4a は作品の一部なので、こちらは追跡する。
_source/bgm/*.mp3

# rembg で背景を抜いた中間ファイル(合計 4MB 超)。元画像から作り直せるので追跡しない。
# 作り直し方は _source/build_adult.py の先頭に書いてある。
_source/*_cut.png
